version: '3.8'

services:
  gps-scraper:
    build: .
    container_name: gps-scraper
    restart: unless-stopped
    environment:
      - USERNAME=LuisDiaZ
      - PASSWORD=Supervision/25
      - TZ=America/Mexico_City
    volumes:
      - ./resultados:/app/resultados
      - ./logs:/app/logs
    command: python gps_scraper.py
